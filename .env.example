# Local dev (with docker compose: docker compose up -d)
DATABASE_URL="postgresql://focus:focus@localhost:5432/focus"

# Production: Railway sets this automatically when you add PostgreSQL
# DATABASE_URL="postgresql://user:pass@host:5432/railway"

DEFAULT_USER_EMAIL="me@example.com"
DEFAULT_USER_NAME="You"
